<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proiect.cornel.comunitatecarti">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/avatar"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.MeniuPrincipalActivity" />
        <activity android:name=".Activities.ProfilActivity" />
        <activity android:name=".Activities.MeniuCartiActivity" />
        <activity android:name=".Activities.BookDetailsActivity" />
        <activity android:name=".Activities.AddBookActivity" />
        <activity android:name="org.achartengine.GraphicalActivity" />
        <activity android:name=".Activities.PieChartActivity"></activity>
    </application>

</manifest>